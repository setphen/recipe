import {
  colors,
  baseFontFamily,
  baseFontSize,
  breakpoints,
  spacing,
  borderRadius,
  borderWidth,
  lineHeights,
  fontSizes,
  fontWeights,
  fonts,
  pageContentWidth,
} from './standard';

export {default as styled, useTheme, mq} from './styled';

export const standard = {
  colors,
  baseFontFamily,
  baseFontSize,
  breakpoints,
  spacing,
  borderRadius,
  borderWidth,
  lineHeights,
  fontSizes,
  fontWeights,
  fonts,
  pageContentWidth,
};
