import {
  colors,
  baseFontFamily,
  baseFontSize,
  breakpoints,
  spacing,
  borderRadius,
  borderWidth,
  lineHeights,
  fontSizes,
  fontWeights,
  fonts,
  pageContentWidth,
} from './standard';

export const standard = {
  colors,
  baseFontFamily,
  baseFontSize,
  breakpoints,
  spacing,
  borderRadius,
  borderWidth,
  lineHeights,
  fontSizes,
  fontWeights,
  fonts,
  pageContentWidth,
};
